# Agent Instructions

This `agents.MD` file provides context and rules for AI agents working in this repository. 

## Project Overview
This repository contains the portfolio website for **Isak Darke**, a QA / Game Tester. 
It is built using vanilla HTML, CSS, and some JavaScript. It does not appear to use a complex framework like React or Next.js. 

### Directory Structure
- `index.html`: The main homepage of the portfolio.
- **Project/Section Folders**:
  - `aboutme/`
  - `chensoclub/`
  - `coldsignal/`
  - `emojidetective/`
  - `experience/`
  - `growbot/`
  - `priceofsanity/`
  - `sd-dp/`
- `cdn-cgi/`: Likely contains Cloudflare or CDN-related assets/scripts.

## Guidelines for AI Agents
When performing tasks in this repository, follow these rules:
1. **Maintain Consistency**: Follow the existing vanilla HTML/CSS architecture. Do not introduce compile steps, frameworks (e.g., React, Vue), or utilities (e.g., Tailwind) unless explicitly instructed by the user.
2. **Design Language**: When adding new project pages, mimic the styling, layout, fonts (Play, Plus Jakarta Sans, Press Start 2P), and color schemes used in `index.html`.
3. **SEO & Meta Tags**: Ensure all new HTML pages retain the required `meta` tags for SEO, OpenGraph, and Twitter Cards to keep the portfolio properly indexed.
4. **Workflows**: Any structured task blueprints should be defined inside `.agents/workflows/` (e.g., how to add a new project).

---
*(AI Note to User: This file has been initialized with basic context about your portfolio. Please feel free to add more specific rules or requests here for future agent tasks!)*